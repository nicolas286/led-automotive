---
import Button from '~/components/ui/Button.astro';

interface SpecItem {
  label: string;
  value: string;
}

interface Props {
  brand?: string;
  title: string;
  excerpt?: string;
  priceLabel?: string;
  promoPriceLabel?: string;
  image: string;
  gallery?: string[];
  primaryCtaHref?: string;
  primaryCtaText?: string;
  secondaryCtaHref?: string;
  secondaryCtaText?: string;
  shortSpecs?: SpecItem[];
}

const {
  brand,
  title,
  excerpt,
  priceLabel,
  promoPriceLabel,
  image,
  gallery = [],
  primaryCtaHref,
  primaryCtaText,
  secondaryCtaHref,
  secondaryCtaText,
  shortSpecs = [],
} = Astro.props;

const visibleGallery = gallery.filter((img) => img && img !== image);
const isPromo = !!promoPriceLabel;
---

<div class="grid gap-10 lg:grid-cols-2 lg:items-start">
  <div>
    <div class="productDetailMainImage">
      <img
        src={image}
        alt={title}
        class="w-full object-cover"
      />
    </div>

    {visibleGallery.length > 0 && (
      <div class="mt-4 grid grid-cols-2 gap-4 sm:grid-cols-4">
        {visibleGallery.map((galleryImage) => (
          <div class="productDetailThumb">
            <img
              src={galleryImage}
              alt={title}
              class="h-28 w-full object-cover"
              loading="lazy"
            />
          </div>
        ))}
      </div>
    )}
  </div>

  <div>
    {brand && (
      <p class="text-sm font-semibold uppercase tracking-[0.18em] text-primary">
        {brand}
      </p>
    )}

    <h1 class="mt-2 text-4xl font-bold tracking-tight text-white md:text-5xl">
      {title}
    </h1>

    {excerpt && (
      <p class="mt-4 max-w-2xl text-lg leading-8 text-slate-300">
        {excerpt}
      </p>
    )}

    {(priceLabel || promoPriceLabel) && (
      <div class="mt-6 flex flex-wrap items-center gap-3">
        {isPromo && priceLabel && (
          <span class="text-lg text-slate-400 line-through md:text-xl">
            {priceLabel}
          </span>
        )}

        <span class="text-3xl font-bold text-primary">
          {promoPriceLabel ?? priceLabel}
        </span>
      </div>
    )}

    {(primaryCtaHref || secondaryCtaHref) && (
      <div class="mt-8 flex flex-wrap gap-4">
        {primaryCtaHref && primaryCtaText && (
          <Button href={primaryCtaHref} variant="primary">
            {primaryCtaText}
          </Button>
        )}

        {secondaryCtaHref && secondaryCtaText && (
          <Button href={secondaryCtaHref}>
            {secondaryCtaText}
          </Button>
        )}
      </div>
    )}

    {shortSpecs.length > 0 && (
      <div class="mt-10 grid gap-4 sm:grid-cols-2">
        {shortSpecs.map((spec) => (
          <div class="productDetailSpecCard">
            <p class="text-sm text-slate-400">{spec.label}</p>
            <p class="mt-1 font-semibold text-white">{spec.value}</p>
          </div>
        ))}
      </div>
    )}
  </div>
</div>

<style>
  .productDetailMainImage {
    overflow: hidden;
    border-radius: 1.75rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.02);
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.22);
  }

  .productDetailThumb {
    overflow: hidden;
    border-radius: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.02);
  }

  .productDetailSpecCard {
    border-radius: 1.25rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.03);
    padding: 1rem;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.18);
  }
</style>